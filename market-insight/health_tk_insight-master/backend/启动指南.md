# 启动服务指南 - 端口8100

本指南说明如何在8100端口启动Market Insight Agent服务。

## 📋 前置要求

- Python 3.8+
- pip (Python包管理器)

## 🚀 快速启动

### 方式一：使用启动脚本（推荐）

#### Linux/Mac:
```bash
cd /path/to/market-insight/health_tk_insight-master/backend
./start.sh
```

#### Windows:
```cmd
cd \path\to\market-insight\health_tk_insight-master\backend
start.bat
```

启动脚本会自动：
- 检查并创建`.env`配置文件
- 检查Python环境
- 安装必要的依赖（如果需要）
- 在8100端口启动服务

### 方式二：手动启动

1. **进入后端目录**
```bash
cd market-insight/health_tk_insight-master/backend
```

2. **安装依赖**
```bash
pip install -r requirements.txt
```

3. **配置环境变量**
```bash
# Linux/Mac
cp .env.example .env

# Windows
copy .env.example .env
```

编辑`.env`文件，确保`API_PORT=8100`

4. **启动服务**
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8100 --reload
```

### 方式三：使用Docker Compose

```bash
cd market-insight/health_tk_insight-master/backend
docker-compose up
```

服务会在8100端口启动。

## 📝 访问服务

服务启动成功后，可以通过以下地址访问：

- **API文档 (Swagger UI)**: http://localhost:8100/docs
- **API文档 (ReDoc)**: http://localhost:8100/redoc
- **健康检查**: http://localhost:8100/health

## 🔧 配置说明

配置文件位于 `backend/.env`，主要配置项：

```env
# API服务配置
API_HOST=0.0.0.0
API_PORT=8100          # 服务端口
API_PREFIX=/api/v1

# 应用配置
APP_ENV=development    # 环境: development | staging | production
DEBUG=true
LOG_LEVEL=INFO
```

## 📦 主要API端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/v1/brand-health` | POST | 品牌健康度分析 |
| `/api/v1/tiktok-insight` | POST | TikTok社媒洞察 |
| `/api/v1/tasks/{task_id}` | GET | 查询任务状态 |

## 🛠️ 故障排查

### 端口已被占用
如果8100端口已被占用，可以修改`.env`文件中的`API_PORT`为其他端口：
```env
API_PORT=8101
```

### 依赖安装失败
尝试升级pip后重新安装：
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Python版本不兼容
确保使用Python 3.8或更高版本：
```bash
python --version
```

## 📞 支持

如有问题，请查看：
- 项目文档：`backend/README.md`
- 快速入门：`backend/QUICKSTART.md`
- API文档：http://localhost:8100/docs （服务启动后）
